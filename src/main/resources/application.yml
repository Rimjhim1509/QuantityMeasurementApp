spring:
  datasource:
    url: jdbc:mysql://localhost:3306/qma_db
    username: root
    password: Rimjhim@1509

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${google_client_id}
            client-secret: ${google_client_secret_key}

  jpa:
    hibernate:
      ddl-auto: update

security:
  jwt:
    secret: ${jwt-secret-key}
